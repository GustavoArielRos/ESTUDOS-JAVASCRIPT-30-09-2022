// COMBINANDO ARRAYS

const numeros = [1,2,3,4,5,];

const combinado = numeros.join('.'); // entre cada elemento vai ter 1 '.'

const frase = "ola , bem vindo";
const resultado = frase.split(' '); // separou baseado em frases ["ola", ",", "bem", "vindo"]
